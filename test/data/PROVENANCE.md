# Test data

`cases/*.json` are mapping scenarios: an instruction, a context and the result the rules in the README give for it, written from an independent reading of those rules over the bundled documents, or over the inline documents a case carries. Each case is named for what it pins. A case that stops matching its document is a rule change, and the case moves with the rule.
